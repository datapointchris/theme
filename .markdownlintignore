# Generated by go-semantic-release on every release, which writes a blank line
# after each version heading. Linting it means --fix strips those on the next
# commit and the release puts them back — permanent churn over formatting no
# one authors by hand.
CHANGELOG.md

# The only three files under analysis/ that a script writes — their table
# separator style comes from the generator, so linting them means editing
# output the next run overwrites. The other 26 docs there are hand-authored
# and are linted normally.
analysis/CANONICAL_COMPARISON_RESULTS.md
analysis/CANONICAL_ML_EXPERIMENT.md
analysis/NEOVIM_COMPARISON_RESULTS.md

# Planning documents - ephemeral, not committed
.planning/**

# Auto-generated noise directories (gitignored)
debug/**
file-history/**
plans/**
shell-snapshots/**
sessions/**
