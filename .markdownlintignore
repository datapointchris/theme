# Preventive: no release here writes a changelog. The entry stands so that one
# arriving generated is never linted, since normalization of a generated file
# is undone on the next release and returns as a conflict.
CHANGELOG.md

# Planning documents - ephemeral, not committed
.planning/**

# Auto-generated noise directories (gitignored)
debug/**
file-history/**
plans/**
shell-snapshots/**
sessions/**
